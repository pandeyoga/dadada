# Test Credentials
# Agent writes here when creating/modifying auth credentials (admin accounts, test users).
# Testing agent reads this before auth tests. Fork/continuation agents read on startup.

DESIGN STUDIO — HOLD · LABEL PROOFING · RIWAYAT DETAIL (2026-09-18): layar `?view=rnd-designs&entity=ent_ksc`.
Nilai desain HANYA saat ACC (dialog `design-action-approve` → `design-action-score-<nilai>` wajib; minta revisi tanpa nilai; endpoint `/versions/{v}/score` DIHAPUS).
HOLD (izin `rnd.hold`, bawaan admin+manager, dapat diatur di matriks hak akses): tombol `design-action-hold` (alasan wajib di `design-action-note`) hanya untuk status approved/final_submitted/active; saat hold: `design-action-release_hold`, banner `design-detail-hold-banner`, lencana `design-detail-hold` / kartu `design-hold-<id>`. Desain HOLD ditolak 400 saat dipakai `POST /api/rnd/samples` jenis proofing.
API: POST /api/design-gallery/{id}/lifecycle/hold {note} · POST …/lifecycle/release-hold {note} · GET …/history?kind= · GET /api/design-gallery?on_hold=yes|no. Tiap desain punya `proofing: {state none|in_progress|finished|master, label, detail, samples[], specs[], master_product}`.
Daftar: KPI klik `rnd-designs-kpi-hold|proofing|master`; filter `rnd-filter-hold`, `rnd-filter-proofing`; navigasi tanggal `design-date-nav` (`-field` ACC/update, `-preset-today|7d|30d|month|prev_month`, `-prev-month`/`-next-month`, `-from`/`-to`, `-clear`). Kartu: `design-proofing-<id>`, `design-master-<id>`, `design-dates-<id>`.
Detail: tab `design-tab-proofing` (panel `design-proofing-panel`, `design-proofing-state`, `design-master-product`, `design-proofing-sample-<smpId>`), tab `design-tab-history` (`design-history-panel`, filter `design-history-filter-all|lifecycle|files|edits|hold|rnd|feedback`, cari `design-history-search`, baris `design-history-<event>`, diff `design-history-changes`).
Data demo: SRI-BGA-AO-001 (dsgn_87a63c89bc80) HOLD + proofing berjalan SMP-00005 · SRI-BGA-AO-002 (dsgn_7eec40ef4396) master produk RND-KTN-150 via KSC/SPEC-00001 · DWI-GEN-002 (dsgn_00ad22fda7bb) proofing selesai SMP-00002. Seed ulang: `cd /app/backend && python ../scripts/seed_design_hold_proofing_demo.py`.
Frontend dilayani dari bundle statis — setelah ubah src: `bash /app/scripts/rebuild_frontend.sh`.


SINKRON PERMINTAAN DESAIN ↔ DESIGN STUDIO (2026-09-18): layar `?view=design-requests&entity=ent_ksc`.
Buat permintaan (admin/manager/md/sales_admin): `dsr-create-button` → `dsr-category-select` (Kategori Pattern, opsi `dsr-category-select-option-<KODE>`), `dsr-dcategory-select`, `dsr-brief-input`, `dsr-assignee-select`, `dsr-due-input`, `dsr-create-submit`.
Detail (`dsr-card-<id>` / `dsr-detail-<id>`): stepper `dsr-stepper`, panel `dsr-linked-designs`; desainer yang ditugaskan (status assigned/in_progress/revision): `dsr-new-design-title` + `dsr-create-design-button` (membuat desain Studio → pindah ke halaman `design-detail-page`, chip `design-detail-request`), atau `dsr-mode-link` → `dsr-link-design-select` + `dsr-link-design-button`.
Status permintaan MENGIKUTI desain Studio: Ajukan (design-action-submit) → "Menunggu keputusan"; Minta revisi → "Minta revisi"; ACC → "Disetujui". Manager di detail permintaan tertaut Studio melihat `dsr-decide-in-studio` + `dsr-open-studio-button` (approve/reject langsung di permintaan ditolak 400 bila tertaut Studio).
API: GET /api/design-requests/meta (categories.pattern/design) · POST /api/design-requests {category_code, design_category_code, brief, assigned_to, submit_now} · POST /api/design-requests/{id}/create-design {title} · POST /api/design-requests/{id}/link-design {gallery_id} · GET /api/design-requests/{id} → designs[], studio_linked.
Seed DSR demo: DSR-00006 (ditugaskan ke Sari), 00007 (dikerjakan, desain draf), 00008 (menunggu keputusan, desain diajukan), 00009 (ACC lewat revisi).
REFERENSI BRIEF (2026-09-18): pembuat permintaan (admin/manager/md/sales_admin) melampirkan gambar → modal buat `dsr-create-refs-upload` (input file, multiple; pratinjau `dsr-create-refs-local-<i>`), detail `dsr-references-upload` / `dsr-references-item-<fileId>` / `dsr-references-delete-<fileId>`; desainer hanya melihat (403 saat unggah). API: POST /api/design-requests/{id}/references (multipart `file`, hanya gambar) · GET …/references/{fid} · DELETE …/references/{fid}. Saat desainer "Buat & buka desain" atau tautkan desain, referensi DISALIN ke desain sebagai berkas kind `reference` (tab Referensi, `design-files-reference`, idempoten via `source_reference_id`); referensi yang ditambah setelah tertaut juga ikut.

PERMINTAAN SAMPLE R&D — UI BARU (2026-09-18): layar `?view=rnd-samples&entity=ent_ksc` kini bergaya papan Permintaan Desain. Pembuat: admin/manager/sales/md (`rnd-sample-create-button` → modal `sample-form-modal` 3 bagian: chip jenis `sample-type-labdip|handfeel|proofing`, `sample-title-input`, `sample-color`, `sample-design`, `sample-so`, `sample-qty-input`, `sample-brief-input`, `sample-target-date`, simpan `sample-form-save`). Papan `rnd-samples-board` kolom `rnd-samples-col-<draft|sent|in_progress|assessed|decided>`, kartu `rnd-sample-card-<id>`; tab `rnd-samples-tab-board|list`; chip jenis `rnd-samples-filter-<code|all>`; chip status `rnd-samples-chip-<status|all>`; KPI `rnd-samples-kpi-total|sent|in-progress|assessed|decided|overdue`.
Rincian (DetailModal `sample-detail-modal`, panel `sample-detail-panel`): stepper `sample-stepper`, TAB PER JENIS `sample-tab-labdip|handfeel|proofing` (panel `sample-tab-panel-<code>`; jenis yang belum diminta → `sample-tab-empty-<code>` + `sample-tab-add-<code>` menambah jenis via PATCH). Aksi: `sample-send-button` (modal `sample-send-modal`: `sample-send-type-<code>`, `sample-send-supplier-<id>`, `sample-send-confirm`), round: `round-upload-<rid>`, `round-submit-<rid>`, `round-assess-<rid>`, `open-round-<supplierId>-<code>`; `sample-decide-button`, `sample-finish-button`, `sample-deliver-button`, `sample-cancel-button` (alasan via dialog askReason), `sample-detail-close`. Mekanisme server (POST /api/rnd/samples, /send, /rounds/{rid}/submit, /assess, /decide) TIDAK berubah.

GALERI BUKTI PER TAB (2026-09-18): di tiap tab jenis rincian sample ada `sample-proof-gallery-<code>` (kolom per supplier `sample-proof-col-<code>-<supplierId>`, thumbnail `sample-proof-thumb-<fileId>`, dimuat via axios blob). Klik thumbnail → lightbox `sample-proof-lightbox` (`-image`, `-caption`, `-prev`, `-next`, `-close`, `-open`). Contoh data: KSC/SMP-00007 (2 supplier, 1 bukti masing²), KSC/SMP-00008 & 00006 (swatch PNG berwarna).

GALERI PER JENIS (2026-09-18): tab hub R&D baru `?view=rnd-labdip|rnd-handfeel|rnd-proofing` (cermin "Desain & Pattern (Master)"). Testid: `sample-gallery-<code>`, judul `sample-gallery-title-<code>`, tombol `sample-gallery-create-<code>` (modal sample-form-modal dgn jenis terkunci "· utama"), KPI `sample-gallery-kpi-total|sent|progress|acc|decided|overdue-<code>`, cari `sample-gallery-search-<code>`, filter `sample-gallery-filter-status|supplier|result|score|revision|proof-<code>`, grid `sample-gallery-grid-<code>`, kartu `sample-type-card-<id>` (sampul `sample-type-cover-<id>`, `sample-type-status-<id>`, `sample-type-score-<id>`), kosong `sample-gallery-empty-<code>`. Klik kartu → `sample-detail-modal` dengan tab jenis itu aktif. Sampel dibatalkan disembunyikan kecuali filter status = Dibatalkan.

Lingkungan lokal pengujian saja. Base URL API: baca REACT_APP_BACKEND_URL di /app/frontend/.env.
Konteks badan usaha uji: ent_ksc (header X-Entity-Id; pilih badan usaha yang sama di layar).

- Admin: admin@kainnusantara.id / demo12345
- MD (merchandiser): md@kainnusantara.id / demo12345
- Manajer: manager@kainnusantara.id / demo12345
- Sales: sales@kainnusantara.id / demo12345
- Admin Sales: salesadmin@kainnusantara.id / demo12345
- Admin Sampel: sampleadmin@kainnusantara.id / demo12345 (Yoga Admin Sampel, role sample_admin, beranda `sample-admin-desk`)
- Finance: finance@kainnusantara.id / demo12345
- Gudang: warehouse@kainnusantara.id / demo12345
- Desainer: designer@kainnusantara.id / demo12345 (Sari Melati, role designer, entitas ent_ksc)

DESIGN STUDIO (2026-09-16): layar `?view=rnd-designs&entity=ent_ksc` (hub Desainer → tab "Desain & Pattern"). Klik kartu `design-card-<id>` membuka halaman detail (`design-detail-page`). Kode desain otomatis (mis. BDI-PTR-SLR-001). Nilai versi 0–2 kelipatan 0,25; ambang ACC default 1,5 (`rnd.design_acc_min_score`). Aksi penilai (review/nilai/ACC/aktifkan/arsip) hanya admin/manager; desainer: buat, unggah, ajukan, versi baru, umpan balik, colorway.

Login UI testid: login-email-input, login-password-input, login-submit-button.
KPI DESAINER (2026-09-17): layar `?view=designer-kpi&entity=ent_ksc` (admin/manager). Tren nilai desain: testid `design-score-trend` (chart `design-score-trend-chart`, months `design-score-trend-months`). Bobot desain: setting `rnd.kpi_weight_design` (PUT /api/config/values, scope_type global). Data demo nilai desain: `cd /app/backend && python ../scripts/seed_design_scores_demo.py`.
MASTER PRODUK SHOWCASE (2026-09-17): `?view=md-products&entity=ent_ksc` → klik `catalog-open-ptpl_c3546aeb9702d12ec9fb` (Endek Bali Rangrang, 4 varian warna, 12 media disetujui: foto/detail/mockup/artwork). Testid: `catalog-family-hero`, `catalog-tab-variants|info|rnd`, `catalog-select-<product_id>`, `media-filter-<all|photo|detail|mockup|artwork>`, `media-item-<id>`, `catalog-relations`, `relations-artworks`. Seed ulang: `cd /app/backend && python ../scripts/seed_endek_showcase.py`.
Routing uji: ?view=md-products&entity=ent_ksc · ?view=rnd-specs&entity=ent_ksc · ?view=sales.
KNSelect: opsi <testid>-option-<value>, pencarian <testid>-search.
Gunakan prefix TEST_ untuk data baru; bersihkan hanya ID uji.
Jika akun md@ hilang setelah seed: `supervisorctl restart backend` (bootstrap idempoten).

FASE SL (scan label): tugas uji `wms_e2e3d9059660` (KSC/PO-00014, CBN-MEGA-PREM, 250 yard, wh_jakarta, bin A1-01).
CATATAN 2026-09-16: task PO-00014 (`wms_7d68b73ebcc1`) sudah DISELESAIKAN oleh main agent untuk data uji selisih → PO `po_78d343ce4efd` status completed dengan `receipt_variances[0]` (roll RL-00058 label≠aktual). Untuk scan label baru pakai task inbound lain yang masih `waiting_goods` (GET /api/inbound/tasks).
Peran `finance` demo TIDAK memegang `vendor_bill.view` (matriks izin lama) → uji Tagihan Supplier / billing-context dengan admin@ atau manager@.
Pola label per Barang Supplier: `sit_b5ed6c7e1bc9` (NTT-IKAT-GRD, Ntt Weaving) berpola GS1; task scan uji `wms_f808e668a2e7` (KSC/PO-00015, TNI-GRGD-001, 180 yard, status receiving) — contoh label GS1 `(240)NTT-IKAT-GRD(10)DL-G1(21)R1(3231)000300` (=30 yd), fallback supplier `NTT-IKAT-GRD|DL-G1|R2|30|5|BLU`. Selalu undo roll uji.
Contoh label: `{"sku":"CBN-MEGA-PREM","lot":"DL-01","roll":"R1","yd":120.5,"kg":25.1,"color":"RED"}` · `CBN-MEGA-PREM|DL-01|R2|118|24.5|RED` · `(240)CBN-MEGA-PREM(10)DL-01(21)R3(3230)00000120`.
Layar: login gudang → `wms-tab-inbound` → `inbound-task-<id>` → panel `scan-label-panel`. Reset demo: `bash scripts/seed_reset.sh`.


§3-C JUAL SAMPEL (2026-09-16, menu "Jual Sampel" DIHAPUS): sampel = baris SO ber-`is_sample` dari POS
(ProductQuickView `quickview-sample-button`, toggle `toggle-sample-<pid>`, harga manual `sample-price-input-<pid>`).
Confirm SO → tugas outbound `task_subtype=sample_cut`; gudang: WMS Barang Keluar → `sample-cut-panel-<taskId>`
(`sample-cut-code-` isi EPC/nomor roll, `sample-cut-length-`, `sample-cut-use-suggested-`) atau HP gudang tab Sampel.
API: GET /api/sample-quote · POST /api/outbound/tasks/{id}/cut-sample {epc|roll_id, actual_length, reason}.
Contoh EPC roll available ent_ksc: lihat rfid_tags (roll RL-00002 → E20B-639D-27E3-C84F-7696-157A).
Uji regresi: pytest backend/tests/test_sample_pos_flow.py. Master harga sampel: view pricelist (bawah).

REVISI SAMPEL (2026-09-17): Pesanan Sampel = SO ber-`order_type:"sample"`, nomor `KSC/SOS-00001` (sequence sendiri, terpisah dari SO-).
POS: ProductQuickView → `quickview-kind-regular` | `quickview-kind-sample` (wajib pilih; sampel qty default = maks: woven 5 yard, knit 2 kg; input di-clamp) → `quickview-sample-button`.
Keranjang satu jenis (banner `pos-cart-kind-banner`, tombol `floating-cart-button` coklat saat sampel). Checkout step 2: `sample-billing-free` | `sample-billing-paid` (wajib) → `checkout-submit`.
API: POST /api/sales-orders {order_type:"sample", sample_billing:"free|paid", items[...]} · GET /api/sample-orders · /desk · /stats/summary · POST /api/sample-orders/{id}/approve-payment (finance|sample_admin|manager|admin) · /confirm (sample_admin|manager|admin → tugas outbound sample_cut) · /cancel.
Server menolak: campur sampel+biasa (SAMPLE_MIXED 400), qty > batas (SAMPLE_LIMIT 400), confirm berbayar sebelum ACC (409 SAMPLE_PAYMENT_PENDING).
Layar: `?view=sample-orders` (testid sample-orders-view, sample-order-row-<id>, sample-detail-approve-payment/confirm/cancel) · `?view=sample-admin-desk` (sample-desk, antrean bayar_sampel/siap_gudang/di_gudang/kirim_ambil/selesai, baris pakai DeskQueueCard testPrefix sample-desk).
Gudang: WMS Barang Keluar → task subtype sample_cut (order_number SOS-) → POST /api/outbound/tasks/{id}/cut-sample {epc|roll_id, actual_length}.
Frontend TIDAK hot-reload: setelah edit src jalankan `bash /app/scripts/rebuild_frontend.sh` (log /app/.frontend_build.log).

REVISI SAMPEL (2026-09-17): Pesanan Sampel = SO ber-`order_type:"sample"`, nomor `KSC/SOS-00001` (sequence sendiri, terpisah dari SO-).
POS: ProductQuickView → `quickview-kind-regular` | `quickview-kind-sample` (wajib pilih; sampel qty default = maks: woven 5 yard, knit 2 kg; input di-clamp) → `quickview-sample-button`.
Keranjang satu jenis (banner `pos-cart-kind-banner`, tombol `floating-cart-button` coklat saat sampel). Checkout step 2: `sample-billing-free` | `sample-billing-paid` (wajib) → `checkout-submit`.
API: POST /api/sales-orders {order_type:"sample", sample_billing:"free|paid", items[...]} · GET /api/sample-orders · /desk · /stats/summary · POST /api/sample-orders/{id}/approve-payment (finance|sample_admin|manager|admin) · /confirm (sample_admin|manager|admin → tugas outbound sample_cut) · /cancel.
Server menolak: campur sampel+biasa (SAMPLE_MIXED 400), qty > batas (SAMPLE_LIMIT 400), confirm berbayar sebelum ACC (409 SAMPLE_PAYMENT_PENDING).
Layar: `?view=sample-orders` (testid sample-orders-view, sample-order-row-<id>, sample-detail-approve-payment/confirm/cancel) · `?view=sample-admin-desk` (sample-desk, antrean bayar_sampel/siap_gudang/di_gudang/kirim_ambil/selesai, baris pakai DeskQueueCard testPrefix sample-desk).
Gudang: WMS Barang Keluar → task subtype sample_cut (order_number SOS-) → POST /api/outbound/tasks/{id}/cut-sample {epc|roll_id, actual_length}.
Frontend TIDAK hot-reload: setelah edit src jalankan `bash /app/scripts/rebuild_frontend.sh` (log /app/.frontend_build.log).

REVISI DESIGNER (2026-09-17, catatan klien): Jenis desain & palet warna DIHAPUS dari form. Dua kategori: Kategori Pattern (SLR/BTK/BGA/PLD/ABS/CSMR) + Kategori Design (AO/PG), master di `design_categories` sumbu `pattern`|`design` (admin/manager tambah via `rnd-designs-categories`). Kode otomatis `{DESIGNER}-{CAT}-{DCAT}-{SEQ}` mis. SRI-SLR-AO-001.
Ronde: request-revision OTOMATIS buka versi baru (round_label "Revisi N", revision_count). Tahap final sesudah ACC: penilai set `final_color_count` (default 4) + `recommended_product_ids` di payload approve; desainer unggah files-kind `colorway` (≥N) + `mockup` (≥1) → POST lifecycle/submit-final → admin lifecycle/activate (hanya dari final_submitted); lifecycle/return-final (catatan wajib) mengembalikan ke approved.
Testid: form `design-title-input`, `design-category-select`, `design-dcategory-select`, `design-code-preview`, `design-form-save`; kartu `design-round-<id>`; detail `design-detail-round`, `design-rounds-progress`, `design-round-<v>`, `design-final-box`, tab `design-tab-final`, `design-final-panel`, `design-final-checklist`, upload `design-upload-colorway`/`design-upload-mockup`/`design-upload-artwork`; aksi `design-action-submit|start_review|request_revision|approve|submit_final|return_final|activate|archive`, dialog `design-action-dialog`, `design-action-note`, nilai `design-action-score-1_75` dst, ACC: `design-approve-products` + `design-approve-color-count`, `design-action-confirm`; peruntukan produk admin (setelah ACC) `design-detail-products-edit`.
Frontend TIDAK hot-reload: setelah edit src jalankan `setsid nohup bash /app/scripts/rebuild_frontend.sh > /app/.rebuild.out 2>&1 &` (background biasa mati saat tool call selesai).

RONDE REVISI SERAGAM (2026-09-17): Permintaan Desain `?view=design-requests&entity=ent_ksc` — badge `dsr-card-revision-<id>`, `dsr-row-revision-<id>`, detail `dsr-detail-revision` + `dsr-rounds-progress`/`dsr-round-<n>` (contoh KSC/DSR-00004 = dsr_a623af63e8e1 → Revisi ke-1). R&D `?view=rnd-samples&entity=ent_ksc` — `rnd-sample-revision-<id>`, detail `sample-detail-revision` (KSC/SMP-00003 → Revisi ke-1). Meja MD — `md-desk-revision-<ref_id>`; API GET /api/md/desk?entity_id=ent_ksc → rows.revision_count.
FILTER REVISI (2026-09-17): `dsr-revision-filter-<all|1|2|3>` di ?view=design-requests (API min_revision=N), `rnd-samples-revision-filter-<all|1|2|3>` di ?view=rnd-samples.
PELANGGAN (2026-09-17): `?view=customers-crm&entity=ent_ksc` → `customer-create-button` → modal seksi `customer-section-store|pic|sales`; sales@ melihat `customer-assigned-sales-self`, admin melihat select `customer-assigned-sales`. Wajib: customer-pic, customer-phone, customer-address. POS quick-add `create-customer-modal` catatan `new-customer-sales-note`.

DISPATCH / LOGISTIK (2026-09-17): layar `?view=logistics&entity=ent_ksc` (admin/manager/warehouse kelola; sales/sales_admin hanya-lihat; driver@kainnusantara.id / demo12345 = sopir Joko Susilo).
Tab: `logistics-tab-dashboard|list|history|fleet`. Dasbor: KPI `dispatch-kpi-<unassigned|waiting-pickup|active|in-transit|eta-today|late|delivered-today>`, aksi cepat `dispatch-create-<shipmentId>` (buka modal buat pengiriman dgn SJ terpilih), `dispatch-handover-<deliveryId>` (serah terima pickup), `dispatch-flow-<status>`, `dispatch-mode-<mode>`, armada `dispatch-veh-*`, baris `dispatch-row-<id>`.
Modal buat: `logistics-mode` (expedition|own_fleet; pesanan ambil → terkunci self_pickup + `logistics-pickup-notice`), `logistics-courier|tracking|service|shipping-cost`, `logistics-vehicle-select` (+ `-option-<vehId>`), `logistics-driver-select`, `logistics-create-submit`.
Detail: `logistics-detail-mode`; pickup → `pickup-panel`, `pickup-code` (admin/manager/sales lihat; gudang/sopir `pickup-code-hidden`), form `pickup-input-code|name|id|note`, `pickup-submit`, `pickup-error`; sukses → status Diserahkan (`pickup-pod-info`).
Riwayat: `history-from|to|mode|status|search|export`, `history-row-<id>`, `history-stat-*`. Armada: `fleet-add-button`, `fleet-plate|type|name|capacity|driver|notes`, `fleet-save`, `fleet-vehicle-<id>`, `fleet-vehicle-toggle-<id>` (perawatan ⇄ tersedia; ditolak saat on_trip), `fleet-driver-<userId>`.
API: GET /api/logistics/dashboard · /history?date_from&date_to&mode&status&q&page · /fleet/availability · GET/POST /fleet/vehicles · PATCH /fleet/vehicles/{id} · POST /fleet/vehicles/{id}/status {status: available|maintenance} · POST /deliveries/{id}/pickup-handover {pickup_code, picker_name, picker_id_no}.
Aturan server: SO fulfillment_method=ambil → mode WAJIB self_pickup (400 bila expedition/own_fleet); SO kirim → self_pickup ditolak; self_pickup tidak punya destination/plat/sopir, transition loaded/in_transit ditolak, hanya pickup-handover (kode salah → 400 + pickup_attempts++); kendaraan own_fleet → on_trip saat in_transit, kembali available saat delivered/failed/completed; kendaraan maintenance/on_trip tidak bisa dipilih.
Anti-bocor: GET /logistics/shipments/unassigned → SJ dari SO ambil punya shipping_address "" + fulfillment_method "ambil"; order_preview (verifikasi Admin Sales) SO ambil → shipping_address kosong + fulfillment_method/pickup_date.
Data demo: `cd /app/backend && python ../scripts/seed_dispatch_demo.py` (idempoten) → SJ KSC/SJ-90007 belum diangkut (SO-0004, kirim), KSC/LG-90004 pickup menunggu diambil (SO-0001, kode KN7PQ4), LG-90001 JNE terlambat in_transit, LG-90002 armada loaded ETA hari ini, LG-90003 delivered, LG-90005 failed; kendaraan B 9021 KNA / B 7710 KNB (tersedia), B 3305 KNC (perawatan). Smoke API: `bash scripts/smoke_dispatch.sh`.

PERAN & HAK AKSES (2026-06): tab `entities-access-tab-roles` di layar `?view=entities-access` (Pengaturan → Badan Usaha & Akses). Panel `role-access-panel`, tombol `role-create-button`, kartu `role-card-<roleId>` (`role-card-users-<id>`, `role-card-kind-<id>`, `role-card-modified-<id>`), grid `role-custom-grid`/`role-builtin-grid`, kosong `role-custom-empty`.
Editor `role-editor-drawer`: `role-editor-label`, `role-editor-description`, `role-editor-copy-from` (KNSelect salin dari peran), `role-editor-all-<none|view|manage>`, baris modul `role-module-row-<moduleId>` dgn tombol `role-level-<moduleId>-<none|view|manage>`, ringkasan `role-editor-count-<level>`, pratinjau `role-menu-preview` (`role-menu-preview-<navId>`), simpan `role-editor-save` → dialog `role-editor-confirm` (`role-editor-confirm-save`, `role-editor-confirm-cancel`), `role-editor-delete` (kustom, hanya bila 0 akun), `role-editor-reset` (bawaan yang diubah), `role-editor-locked` (admin dikunci), `role-editor-error`.
API: GET /api/access/modules · GET/POST /api/access/roles · GET/PATCH/DELETE /api/access/roles/{id} · POST /api/access/roles/{id}/reset (izin permission.view/update = admin). Custom role id berawalan `cr_`. Peran kustom muncul di GET /api/roles (custom:true, nav.add/remove) & pilihan peran di formulir akun.
Modul (24): penjualan, sample_order, customer, product, pricing, purchasing, supplier, payable, interco, rnd, design, warehouse_ops, inventory, inspection, logistics, ar, finance_ops, accounting, petty_cash, hr, documents, reports, approval, admin (sensitif). Tingkat: none/view/manage.
Contoh: peran kustom `cr_staf_penagihan` ("Staf Penagihan", acuan finance) dipakai akun penagihan@kainnusantara.id / demo12345 (Rina Penagihan, ent_ksc).
Peran kustom `cr_wakil_manajer` ("Wakil Manajer", acuan manager, tanpa ubahan) dipakai akun wakilmanajer@kainnusantara.id / demo12345 (Wira Wakil, ent_ksc) — uji pengenalan peran kustom di design-requests / internal-requests / laporan penilaian R&D (backend `role_registry.role_in`, frontend `roleIs` di config/roles.js).
Editor peran (2026-06): daftar akun `role-editor-accounts` (summary `role-editor-users`, item `role-editor-account-<userId>`, kosong `role-editor-accounts-empty`; GET /api/access/roles/{id} → `accounts[]`). Dialog in-app hapus/reset/pindah: `role-editor-danger` (`role-editor-danger-title`, `-cancel`, `-confirm`) — menggantikan window.confirm.
PRATINJAU NOTIFIKASI (2026-06): `role-editor-turn-alerts` kini LIVE mengikuti tingkat yang disunting (POST /api/access/roles/preview {role_id|base_role, levels} → turn_alerts, screens); badge `role-editor-turn-alerts-changed`, item `role-editor-turn-alert-item` ("(baru)" hijau) / `role-editor-turn-alert-lost` (dicoret), kosong `role-editor-turn-alerts-empty`.
DUPLIKAT PERAN: tombol `role-card-duplicate-<roleId>` di kartu (tidak ada untuk admin) → editor mode buat dengan judul "Duplikat Peran · X", nama "X (salinan)", acuan = peran itu (bawaan) / acuan peran itu (kustom).
KONFIGURASI LANJUTAN (2026-06): tiap baris modul punya tombol `role-module-advanced-<moduleId>` → panel `role-advanced-panel-<moduleId>` berisi halaman/bagian (sumber daya izin) `role-resource-row-<resource>` (label `role-resource-label-<resource>`, `role-resource-all-<resource>`, `role-resource-none-<resource>`, badge `role-resource-changed-<resource>`) dan chip aksi `role-action-<resource>-<action>` (aria-pressed). Aksi apa pun otomatis menyalakan `view`; mencabut `view` mematikan semua aksi. Level modul DITURUNKAN dari aksi (label "Kelola penuh · sebagian" bila parsial). Simpan mengirim `permissions` (matriks penuh) ke PATCH/POST /api/access/roles; preview juga menerima `permissions`. GET /api/access/modules → `resource_labels`, `action_labels`.

PINDAH AKUN MASSAL: di `role-editor-accounts` bila ada akun: `role-editor-move` → KNSelect `role-editor-move-target` (opsi `role-editor-move-target-option-<roleId>`; admin tidak ditawarkan) + `role-editor-move-button` → dialog `role-editor-danger` (kind move) → POST /api/access/roles/{id}/move-users {to_role} (400 bila ke admin / peran sama). Editor tetap terbuka & disegarkan (akun 0 → tombol hapus aktif).

SESI 2026-09-18 (lanjutan repo KN) — DESAINER & R&D:
- Design Studio Ringkasan: tab "Ronde & Nilai" DIHAPUS; tab Ringkasan kini linimasa vertikal `design-rounds-timeline` (blok `design-round-block-<v>`: berkas dikirim → `design-round-comment-<v>` komentar revisi → `design-round-acc-<v>` ACC + nilai; unggah berkas ronde berjalan via `design-upload-artwork`). Nilai tetap hanya lewat dialog ACC (`design-action-approve`).
- Tab Final: HANYA mockup (`design-files-mockup`, `design-upload-mockup`) + file desain asli kind `source` (`design-files-source`, `design-upload-source`, unduh `design-file-download-<fid>`; GET files/{fid}?download=1 → Content-Disposition attachment). `final = {mockup_files, mockup_required:1, source_files, source_required:1, complete}`; varian warna tidak lagi wajib. submit-final ditolak bila belum lengkap.
- Galeri Desain (Showcase) `?view=cs-design-gallery`: hanya desain ACC (approved/final_submitted/active), TANPA tombol tambah. Filter `gallery-search`, `gallery-filter-status|category|dcategory|tag|designer|rnd|line`, `gallery-filter-reset`, `gallery-count`; kartu `gallery-card-<id>` → modal `gallery-showcase-modal` (`gallery-showcase-code/title/status/score/hero/artwork/mockup/source/tags/products/rnd/master`, `gallery-showcase-open-studio`).
- R&D Permintaan Sample: tab hub "Spesifikasi Produk" DIHAPUS — form buat sample bagian 3 `sample-section-spec` (`sample-with-spec` → `sample-spec-fields`: induk/varian via SpecVariantFields, `sample-spec-fabric|gramasi|lebar|sku|price|notes`); POST /api/rnd/samples menerima `spec: {...SpecInput}` → spesifikasi dibuat & ditautkan (spec_id).
- Rincian sample: round bergaya linimasa `sample-round-timeline-<supplierId>-<code>` (baris `round-row-<rid>`: dikirim/tenggat/diterima, bukti, catatan hasil `round-meas-<rid>`, keputusan penilai `round-qc-<rid>`). Panel `sample-master-data` (langkah `sample-master-spec|acc|color|product|release`, tombol `sample-master-acc-spec-button`/`-confirm`, `sample-master-release-button`). GET /api/rnd/samples/{id} → `master_data {spec, product, template, contract, supplier_item, color}`.
- Pilih pemenang (`decide-modal`): field baru `decide-supplier-color-name`, `decide-supplier-color-code` (wajib bila labdip & ada warna target), `decide-approve-spec` + `decide-product-sku|name`, pratinjau `decide-impact`. POST decide body: supplier_color_name, supplier_color_code, approve_spec, product_sku, product_name → decision.{supplier_color_*, color_synced, product_sku, product_id, master_error}; warna → `color_library.supplier_variants[]` (+factory_name bila kosong); spec di-ACC → produk lahir (lifecycle disetujui).
- Pemisahan tugas: pembuat sample TIDAK boleh memutus pemenang sendiri → pakai manager@kainnusantara.id / demo12345 untuk decide. Contoh data uji: KSC/SMP-00029 (smp_c4601d33b7ee) sudah decided + master data lengkap; desain ACC: SRI-BGA-AO-001 (dsgn_544db49b7e6c).
- Frontend bundle statis: setelah ubah src jalankan `bash /app/scripts/rebuild_frontend.sh` (log /app/.frontend_build.log).

SESI 2026-09-19 — POP-UP R&D SERAGAM + RIWAYAT WARNA SUPPLIER:
- Semua pop-up R&D (RoundActionModal `round-action-modal`, SampleSendModal `sample-send-modal`, SampleFinishModal `sample-finish-modal|sample-deliver-modal`, IssueMaterialModal `issue-material-modal`, DecideModal `decide-modal`) kini memakai shell `components/FormModal` + `features/rnd/RndField.jsx` (Field/Hint/Footer). Tombol tutup = `<testId>-close` (mis. `decide-modal-close`, `sample-send-modal-close`); tombol konfirmasi tetap: `round-modal-confirm`, `sample-send-confirm`, `sample-finish-confirm`/`sample-deliver-confirm`, `issue-confirm`, `decide-confirm`.
- Pustaka Warna (`?view=color-library`): kartu warna dengan versi supplier menampilkan badge `color-variants-<colorId>` → modal `color-variants-modal` (panel `color-variants-panel`, judul `color-variants-title`, baris `color-variant-<supplierId>`, tautan sampel `color-variant-sample-<supplierId>` → deep-link rincian sample). Data: KN-BLU-01 (col_kn_blu_01) punya 1 versi supplier (Palembang Silk House · Navy 07 · NV-07). `supplier_variants` disimpan satu entri per supplier (versi terbaru).

SESI 2026-09-19b — PUSTAKA WARNA SUB-TAB & KETERKAITAN:
- `?view=color-library`: sub-tab `color-subtabs` → `color-subtab-internal` (kartu warna KN) | `color-subtab-supplier` (tabel `supplier-colors-tab`: `supplier-color-search`, `supplier-color-supplier-filter`, `supplier-color-linked-filter` (Sudah/Belum jadi produk), `supplier-color-count`, baris `supplier-color-row-<colorId>-<supplierId>`, tautan sampel `supplier-color-sample-…`, tombol `supplier-color-links-…`).
- Modal keterkaitan `color-links-modal` (`color-links-panel`, `color-links-title`, kolom `color-links-internal` / `color-links-suppliers` (`color-links-supplier-<supplierId>`, `color-links-sample-<supplierId>`) / `color-links-products` (`color-links-product-<pid>`), daftar `color-links-samples`). Dibuka dari badge kartu `color-variants-<colorId>` (ada di semua kartu) atau tombol Keterkaitan di tab supplier.
- Backend: GET /api/color-library/supplier-variants (rata: warna internal × supplier + produk tertaut via decision.product_sku), GET /api/color-library/{id}/links → {color, supplier_variants[+products], products (via md_specs.color_target.color_id → product_id), samples (md_samples.color_target.color_id)}.
- SupplierColorVariantsModal.jsx DIHAPUS (diganti ColorLinksModal).

SESI 2026-09-19c — SPESIFIKASI WAJIB MENYATU DI FORM LABDIP/HANDFEEL/PROOFING:
- POST /api/rnd/samples WAJIB `spec_id` ATAU `spec{...}` → 400 bila tidak ada. Validasi per jenis: target.fabric_type wajib; labdip → color_target.color_id wajib; proofing → design_id wajib. Spesifikasi mewarisi judul/desain/pesanan/lini/warna/satuan dari sample.
- POST /api/rnd/samples/{id}/spec (SpecInput) → lengkapi spesifikasi untuk sample lama (400 bila sudah punya). GET list → tiap item punya `spec_summary {number,status,fabric_type,gramasi,lebar,template_name,sku_hint,product_sku}` atau null.
- Form `sample-form-modal` 4 langkah: (1) `sample-section-source` (`sample-spec` acuan / "spesifikasi BARU", `sample-so`), (2) `sample-section-spec` WAJIB: `sample-type-picker` + `sample-spec-fields` (`sample-spec-fabric|gramasi|lebar|sku|price|notes`, `sample-color` (labdip wajib), `sample-design` (proofing wajib), `sample-spec-base-fabric-trigger`, handfeel: `sample-spec-handfeel` → `sample-spec-yarn|yarn-system|epi|ppi|composition|finishing|handfeel-target`); pesan `sample-spec-problems`; bila memakai acuan → `sample-spec-linked`. (3) `sample-section-what` (`sample-title-input`, `sample-qty-input`, `sample-unit-input`, `sample-brief-input`), (4) `sample-section-when`. Simpan `sample-form-save` (disabled sampai lengkap).
- Rincian sample: panel `sample-spec-panel` → `sample-spec-summary` + `sample-spec-number|status`, tombol `sample-spec-edit-button` → `sample-spec-edit-modal` (`spec-edit-*`, `sample-spec-edit-save`); data lama tanpa spec → `sample-spec-missing` + `sample-spec-complete-button` → `sample-spec-complete-modal` (`complete-spec-*`, `sample-spec-complete-save`).
- Kartu tab Labdip/Handfeel/Proofing: baris `sample-type-spec-<id>` (Induk · jenis kain · gsm · lebar · SKU) atau "⚠ belum ada spesifikasi".

SESI 2026-09-19d — DROPDOWN "ACUAN SPESIFIKASI" DIHAPUS:
- Form `sample-form-modal` kini 3 langkah: (1) `sample-section-spec` WAJIB (jenis + spesifikasi) · (2) `sample-section-what` brief (+ `sample-so` pesanan pelanggan pindah ke sini) · (3) `sample-section-when`. Testid `sample-spec` (acuan) dan `sample-spec-linked` TIDAK ADA lagi; setiap sample selalu membuat spesifikasi baru (POST body selalu berisi `spec`). Uji bertahap di kain yang sama → "Tambah jenis" di rincian sample (spesifikasi tetap satu).

SESI 2026-09-19e — "DUA WARNA" DI SEMUA MENU:
- Master produk kini menyimpan denormalisasi: `color_ref {id,code,name,hex,factory_name}` (warna internal), `supplier_colors[] {supplier_id,supplier_name,supplier_color_name,supplier_color_code,sample_number}` (versi supplier, pemenang paling depan), `rnd_supplier {id,name,sample_number,contract_number,price,...}`, `rnd_spec_number`. Diisi `color_service.sync_product_colors(pid)` saat decide sample & approve spec; backfill: `python -c "import asyncio; from services import color_service as c; asyncio.run(c.backfill_product_colors())"` di /app/backend.
- Komponen `components/ProductColorChip.jsx` (testid `product-color-<pid>`, `product-color-supplier-<pid>-<supplierId>`; prop supplierId → hanya versi supplier itu). Dipakai di: ProductSelect (semua pemilih produk; prop supplierId), POCreateForm (opsi produk `item-product-select` + baris item), RFQCreateModal (label opsi), CatalogWorkspace header (`catalog-selected-colors`), VariantCard, ProductDetail (`detail-product-colors`, `detail-product-supplier-colors`, `detail-product-rnd-supplier`), SampleMasterDataPanel.
- Contoh: SKU TST-TWL-NVY-01 / induk "Produk Uji" (?view=md-products) → KN-BLU-01 Biru Indigo · Palembang Silk House: Navy 07 (NV-07) · supplier pemenang Palembang Silk House.

SESI 2026-09-19f — SPECIAL ORDER FASE 1 (menu, intake terstruktur, routing, rantai):
- Menu: PENJUALAN → "Pesanan Khusus" (id/view `special-orders`, tepat di bawah Kasir & Portal; tab di hub Pesanan dihapus). Nomor OD: SORD-YYMMDD-000n.
- POST /api/special-orders (permission order.create) body baru: title, request_types [printing|labdip|handfeel], detail_level (reference|full), reference_notes, spec {template_id,fabric_type,gramasi,lebar,color_id,color_new_note,sku_hint,notes} + custom_item lama (description, quantity, unit, target_price) + expected_delivery + submit_for_approval.
- POST /special-orders/{id}/references (multipart file, caption) & GET /special-orders/{id}/references/{fid}; POST /special-orders/{id}/route (approve permission; untuk OD lama sudah disetujui). GET detail → `chain {design_request, design, samples[], products[], pr, so, phase, phases[]}`.
- Saat approve penuh → routing otomatis: printing → design_requests (source customer, special_order_id) + salin referensi; labdip/handfeel → md_specs + md_samples (special_order_id, exclusive_customer_id). Field OD: design_request_id/number, sample_ids/numbers, spec_id, routing_errors, routed_at.
- UI: form `special-order-form` (od-customer, od-title, od-type-printing|labdip|handfeel, od-detail-level, od-reference-notes, od-template/od-fabric/od-gramasi/od-lebar/od-sku (hanya detail full), od-color, od-color-new, od-qty, od-unit, od-price, od-delivery, od-notes, od-submit-approval, od-problems, special-order-save). Rincian: `od-chain-panel` (od-phases/od-phase-<k>, od-spec-summary, od-chain: od-node-design|samples|products|purchasing, od-sample-<id>, od-route-button, od-routing-errors, od-references + od-reference-upload, od-reference-<id>).
- Contoh: SORD-260919-0001 (sord_dab2dcbc0de9, Tekstil Medan Jaya, labdip+handfeel) → sample KSC/SMP-00042 + 1 referensi. Approve OD: manager@kainnusantara.id.
- Fase 2 (belum): ACC pelanggan, harga final, eksklusivitas SKU di Kasir/SO, PR/PO memakai supplier pemenang, hapus "Buat SKU" ad hoc, proofing otomatis setelah desain ACC.

SPECIAL ORDER FASE 2 (2026-09-19): layar `?view=special-orders&entity=ent_ksc` → baris → "Detail →". Panel `od-customer-approval` (tombol `od-customer-acc|revisi|tolak-button`, form `od-customer-form`: `od-customer-date`, `od-customer-contact`, `od-customer-note`, `od-customer-evidence`, `od-customer-submit`; riwayat `od-customer-history`, item `od-customer-decision-<id>`, bukti `od-customer-evidence-<fid>`), panel `od-pricing` (`od-price-margin`, `od-price-final`, `od-price-total`, `od-price-lock-button`, `od-price-locked`, `od-price-unlock-button` admin). Fase baru `od-phase-pricing`.
API: POST /api/special-orders/{id}/customer-decision {decision acc|revisi|tolak, note, decided_at, contact_name} · POST …/customer-decision/evidence (multipart file, decision_id) · GET …/pricing?margin_pct= · POST …/lock-price {margin_pct, note} (sales/sales_admin/manager/admin; wajib customer_decision=acc) · POST …/unlock-price {reason} (admin saja).
Eksklusivitas: produk hasil OD punya `exclusive_customer_id`; POST /api/sales-orders untuk pelanggan lain → 400 "EKSKLUSIF untuk pelanggan …". Badge `product-customer-exclusive-badge-<id>` (POS) / `catalog-customer-exclusive-<id>` (katalog).
Proofing otomatis: desain dari OD printing di-ACC (POST /api/design-gallery/{id}/lifecycle/approve) → sample proofing lahir, masuk `sample_ids` OD.
Data uji: OD SORD-260919-0009 (sord_3cea851d93c6) sudah ACC pelanggan + harga dikunci (42.000 + 35% = 56.700), produk eksklusif prod_9497b822a098 (MTO-260919-0009) milik cust_textile_medan; pelanggan lain cust_toko_kain. OD SORD-260919-0008 printing → desain dsgn_10b40ccfd5c3 ACC → proofing KSC/SMP-00047. OD SORD-260919-0007 (sord_607ea61709a4, sample smp_1213cb9c0c99 masih draft) & 0004 (smp_d016417c74c6) tersedia untuk alur baru. Aturan pemisahan tugas: pembuat sample tidak boleh memutus pemenang → pakai manager@ untuk /decide bila sample dibuat admin. Labdip measurements wajib: delta_e, colorfastness_wash, colorfastness_rub.

SPECIAL ORDER FASE 3 (2026-09-19): POST /api/special-orders/{id}/submit (draft → pending_approval, tombol `submit-special-order-btn`); lock-price kini menerima {margin_pct, note, warehouse_id, auto_po} → PR (source special_order, created_by "Sistem (OD)") → approve → PO ke supplier pemenang (harga kontrak, tenggat OD), OD → in_production; ulang manual POST …/procure {warehouse_id}. Supplier grup (Kanda Fabric, CV Cipta Sandang) DITOLAK oleh aturan antar-entitas — pakai supplier luar (Solo Weave sup_343fce6af69e, Palembang Silk House sup_a05908473e52). SKU eksklusif otomatis dirilis ke produksi saat pengadaan. PO selesai diterima (recompute_po_status completed) → OD ready. convert-to-so (in_production/ready + harga terkunci) → SO memakai harga final (item.special_order_price true, cost_price, margin_pct). Form OD printing: `od-pattern-category`, `od-design-category` (fallback kategori pertama aktif). UI pricing: `od-price-warehouse`, `od-price-autopo`, `od-po-chip`, `od-procure-button`.
Data uji: SORD-260919-0001 (sord_dab2dcbc0de9) in_production, PO KSC/PO-00016 Solo Weave, SO KSC/SO-00016 harga 49.400 (38.000+30%). SORD-260919-0007 (sord_607ea61709a4) PR-00009 tapi PO gagal (supplier grup Kanda). OD baru sord_4ec24d652f6f printing → DSR kategori ABS/AO otomatis.

SPECIAL ORDER FASE 4 (2026-09-19): pengiriman otomatis — saat lock-price (auto_po) SO OD ikut lahir otomatis; PO OD diterima gudang (scan-receive + complete; bila QC → qc-decision accept) → stok direservasi ke SO, SO confirmed, tugas Surat Jalan (wms outbound) lahir; gudang scan-pick + dispatch → SJ terbit → OD shipped; SO mark-delivered / logistik delivered → OD done. Ulang manual: POST /special-orders/{id}/prepare-shipment (tombol `od-prepare-shipment-button`, panel `od-shipping`, `od-outbound-tasks`, `od-shipments`, `od-shipping-error`). Meja Admin Sales (GET /api/sales-admin/desk): antrean `od_acc_pelanggan`, `od_kunci_harga`, `od_siap_kirim` (ref_type special_order → deep-link ke rincian OD). Data: sord_ea3ce1b8387d SORD-0020 sudah done (SJ KSC/SJ-00005); sord_1f6b5ccddd31 SORD-0023 menunggu ACC; sord_1af078aa3771 SORD-0024 perlu kunci harga.

MARKETING & SOSMED (2026-09-19): menu "Marketing & Sosmed" (hub marketing-hub; tab views mkt-calendar / mkt-campaigns / mkt-analytics), peran admin/manager/designer/sales/sales_admin. API: GET /api/marketing/meta, /campaigns (+POST/PATCH), /posts (filter month=YYYY-MM, platform, status, campaign_id; POST/PATCH/DELETE), POST /posts/{id}/transition {to, note, published_url} (idea→draft→review→approved(manager/admin)→scheduled(butuh publish_at+PIC)→published), POST /posts/{id}/metrics {metrics:{likes,comments,shares,saves,reach,impressions,clicks,followers_gained,sales_leads}} (hanya published), POST /posts/{id}/attachments (multipart), GET /assets?q=, GET /analytics?month=. Scheduler job content_reminder (15 mnt) → notifikasi PIC. testid: mkt-new-post, mkt-post-form, mkt-f-title, mkt-f-platform-instagram, mkt-f-publish-at, mkt-f-pic, mkt-f-caption, mkt-post-submit, mkt-post-detail, mkt-action-{to}, mkt-published-url, mkt-metric-likes, mkt-save-metrics, mkt-mode-list, mkt-day-YYYY-MM-DD, mkt-post-chip-{id}, mkt-new-campaign, mkt-c-name, mkt-campaign-submit, mkt-analytics, mkt-kpi-*. Data uji: kampanye mkc_2fdaaf6a5b4f "Lebaran 2027", post mkp_29f459d25e24 (published, Sep 2026).

MARKETING LANJUTAN (2026-09-19): Akun Sosmed per PT: GET/POST /api/marketing/accounts, PATCH /accounts/{id} (dibuat untuk badan usaha aktif dari header X-Entity-Id; dup handle/platform per PT → 400; post account_ids harus akun PT yang sama & platform cocok). Dashboard: GET /api/marketing/dashboard?month=YYYY-MM&entity_id=all|ent_xxx (kpi, kpi_prev, trend[6], by_platform, by_account, by_entity, top_posts, upcoming, overdue, pipeline). PDF: GET /api/marketing/calendar.pdf?month=&entity_id= (application/pdf). Multi-entitas: posts/accounts/dashboard menerima entity_id=all (gabungan sesuai allowed) atau tanpa param = PT aktif; setiap post punya entity_name. UI: tab hub marketing-hub = Kalender Konten (mkt-export-pdf, badge mkt-entity-badge-{id} saat header PT = Semua), Dashboard & Performa (mkt-dashboard, mkt-kpi-*, mkt-trend, mkt-by-platform/-account/-entity, mkt-upcoming), Kampanye, Akun Sosmed (mkt-accounts, mkt-new-account, mkt-a-platform-*, mkt-a-handle, mkt-account-submit). Form post: chip akun mkt-f-account-{id} muncul setelah platform dipilih. Data: akun mka_ad980be24b00 @kainnusantara.id (ent_ksc), post Kanda (ent_kanda) Sep 2026 "Konten Kanda".

PERBAIKAN FOKUS INPUT FORM R&D (2026-09-21): `SampleSpecFields.jsx` — komponen pembungkus `L` dipindah ke luar fungsi render (dulu dibuat ulang tiap render → input gramasi/lebar/SKU/handfeel/harga/catatan kehilangan fokus tiap 1 karakter di form Labdip/Handfeel/Proofing `sample-form-modal` dan panel `SampleSpecPanel`). Pola yang sama dirapikan di `design/FinalPanel.jsx` (`Row`).
Frontend dilayani dari bundle statis — setelah ubah src wajib: `bash /app/scripts/rebuild_frontend.sh` (jalankan dengan setsid/nohup, ±8-10 menit).
Login uji: admin@kainnusantara.id / demo12345 (entity ent_ksc). Layar: `?view=rnd-samples&entity=ent_ksc` → `rnd-sample-create-button`.
